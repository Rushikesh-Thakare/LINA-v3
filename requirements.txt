# LINA v3 — Python Dependencies
# NEVER add: torch, transformers, faiss, langchain (Gemini.md strict rule)
#
# NOTE: openwakeword is intentionally NOT listed here.
# It must be installed with --no-deps to avoid pulling tflite-runtime,
# which has no Python 3.12 wheel on PyPI.
# Use setup_venv.sh to install everything correctly.

# UI
PyQt5>=5.15

# Speech-to-Text (offline, fast)
faster-whisper>=1.0.0

# AI Brain
anthropic>=0.40.0
groq>=0.11.0

# Audio
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.11.0
webrtcvad>=2.0.10

# TTS (offline primary: piper Python API — no external binary needed)
piper-tts>=1.4.0
edge-tts>=7.0.0
pyttsx3>=2.90

# System / utils
python-dotenv>=1.0.0
psutil>=5.9.0
setuptools>=65.0       # required by webrtcvad (pkg_resources)
requests>=2.28.0       # required by openwakeword
scikit-learn>=1.0,<2   # required by openwakeword

# openWakeWord ONNX runtime (installed directly, not via openwakeword's deps)
onnxruntime>=1.16.0
